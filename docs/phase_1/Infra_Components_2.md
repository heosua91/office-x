# Infrastructure Components List (VM-based)

Danh sách các thành phần hạ tầng chi tiết dựa trên kiến trúc `Infrastructure_2.md` (EC2 + Auto Scaling).

| No | リソース (Resource) | 環境 (Environment) | 言語 (Language) | フレームワーク (Framework) | 説明 (Description - Vietnamese) | Description (English) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | **Amazon VPC** | AWS Global | N/A | N/A | Mạng riêng ảo cô lập tài nguyên của hệ thống, bao gồm cấu hình mạng con (Subnets) và định tuyến (Routing). | Virtual Private Cloud isolating system resources, including subnets and routing configuration. |
| 2 | **NAT Gateway** | AWS (Public Subnet) | N/A | N/A | Cổng kết nối giúp các server trong mạng nội bộ (Private) truy cập Internet để tải cập nhật/thư viện mà không bị truy cập từ bên ngoài. | Gateway allowing private subnet servers to access the Internet for updates/libraries without external access exposure. |
| 3 | **Amazon CloudFront** | AWS Global | N/A | CDN | Mạng phân phối nội dung (CDN) giúp tăng tốc độ tải file tĩnh (JS/CSS) và streaming video cho người dùng toàn cầu. | Content Delivery Network (CDN) accelerating static file (JS/CSS) delivery and video streaming for global users. |
| 4 | **Application Load Balancer (ALB)** | AWS (Public Subnet) | N/A | N/A | Cân bằng tải, phân phối lưu lượng truy cập từ người dùng (HTTPS) đến các Web Server bên trong. | Load balancer distributing HTTPS traffic from users to internal Web Servers. |
| 5 | **Bastion Host** | AWS (Public Subnet) | Linux (Bash) | SSH | Máy chủ trung gian giúp quản trị viên truy cập an toàn vào các hệ thống trong mạng nội bộ. | Intermediary server allowing administrators secure access to internal network systems. |
| 6 | **Amazon EC2 (Web App Tier)** | AWS (Private Subnet) | Node.js | Next.js / NestJS | Cụm máy chủ ứng dụng chính, xử lý API và giao diện Web. Tự động mở rộng (Auto Scaling) theo mức độ sử dụng CPU. | Main application server cluster handling APIs and Web UI. Auto-scales based on CPU usage. |
| 7 | **Amazon EC2 (Worker Tier)** | AWS (Private Subnet) | Node.js | BullMQ / Celery | Cụm máy chủ xử lý tác vụ nền (Background Jobs) như: gọi AI, xử lý văn bản. Tự động mở rộng theo độ dài hàng đợi (Redis). | Background job processing cluster (AI calls, text processing). Auto-scales based on queue length (Redis). |
| 8 | **Amazon EC2 (Socket Tier)** | AWS (Public/Private) | Node.js | Socket.io / WS | Cụm máy chủ xử lý kết nối thời gian thực (Realtime), Chat, và tín hiệu cuộc gọi. Sử dụng Redis Adapter để scale. | Realtime connection handling cluster (Chat, Signaling). Uses Redis Adapter for scaling. |
| 9 | **Amazon EC2 (Media Tier)** | AWS (Public Subnet) | Go / C++ | LiveKit / Coturn / WebRTC | Cụm máy chủ chuyên dụng xử lý luồng Video/Audio thời gian thực. Sử dụng Spot Instance để tiết kiệm chi phí. Đặt tại Public để tối ưu tốc độ. | Dedicated server cluster for real-time Video/Audio streaming. Uses Spot Instances for cost savings. Placed in Public Subnet for speed optimization. |
| 10 | **Amazon RDS PostgreSQL** | AWS (Private Subnet) | SQL | PostgreSQL 15+ | Cơ sở dữ liệu quan hệ lưu trữ thông tin người dùng, lịch họp, biên bản. Cấu hình Multi-AZ để đảm bảo tính sẵn sàng cao. | Relational database storing user info, meeting schedules, minutes. Multi-AZ configured for High Availability. |
| 11 | **Amazon ElastiCache** | AWS (Private Subnet) | N/A | Redis | Bộ nhớ đệm (Cache) giúp tăng tốc độ truy xuất dữ liệu và quản lý hàng đợi (Queue) cho các Worker hoặc WebSocket state. | In-memory cache for faster data access and queue management for Workers or WebSocket states. |
| 12 | **Amazon S3** | AWS Global | N/A | N/A | Dịch vụ lưu trữ đối tượng, dùng để chứa file ghi âm, video, ảnh đại diện và các tệp tĩnh (Static Assets). | Object storage service for recording files, videos, avatars, and static assets. |
| 13 | **AWS SES / Sendgrid** | External Cloud | SMTP / API | N/A | Dịch vụ gửi Email giao dịch (OTP/Thông báo) và tin nhắn SMS xác thực. | Transactional Email (OTP/Notifications) and SMS authentication service. |
| 14 | **AWS CodeDeploy** | AWS Global | N/A | N/A | Dịch vụ tự động hóa việc triển khai mã nguồn mới lên các cụm Auto Scaling Group mà không gây gián đoạn dịch vụ. | Service automating code deployment to Auto Scaling Groups with zero downtime. |
| 15 | **Amazon CloudWatch** | AWS Global | N/A | N/A | Dịch vụ giám sát, thu thập log và các chỉ số (Metrics) để cảnh báo và kích hoạt Auto Scaling. | Monitoring service collecting logs and metrics for alarms and Auto Scaling triggers. |
| 16 | **AWS IAM** | AWS Global | N/A | N/A | Quản lý định danh và quyền truy cập vào tài nguyên AWS (Identity & Access Management). | Identity and Access Management for AWS resources. |
| 17 | **AWS KMS** | AWS Global | N/A | N/A | Dịch vụ quản lý khóa, dùng để mã hóa dữ liệu trong DB, S3 và EBS volumes (Key Management Service). | Key Management Service for encrypting data in DB, S3, and EBS volumes. |
| 18 | **AWS Secrets Manager** | AWS Global | N/A | N/A | Dịch vụ lưu trữ và quản lý các thông tin nhạy cảm như mật khẩu DB, API Key, Certificate. | Service for storing and managing sensitive information like DB passwords, API Keys, Certificates. |
| 19 | **AWS OpenSearch** | AWS (Private Subnet) | N/A | Elasticsearch / OpenSearch | Công cụ tìm kiếm và phân tích dữ liệu log, hoặc tìm kiếm full-text nội dung cuộc họp. | Search and analytics engine for logs or full-text search of meeting content. |
| 20 | **Google Calendar API** | Google Cloud | REST API | OAuth 2.0 | Dịch vụ lịch bên ngoài, được tích hợp để đồng bộ lịch họp 2 chiều với Office X. | External calendar service integrated for 2-way meeting schedule synchronization. |
| 21 | **Outlook Calendar API** | Microsoft Graph | REST API | OAuth 2.0 | Dịch vụ lịch bên ngoài (Microsoft 365), tích hợp đồng bộ lịch họp. | External calendar service (Microsoft 365) integrated for meeting schedule synchronization. |
| 22 | **OpenAI / AmiVoice API** | External Cloud | REST API | Whisper / AmiVoice | Cung cấp khả năng trí tuệ nhân tạo: Chuyển đổi giọng nói thành văn bản (Speech-to-Text) và Tóm tắt/Phân tích biên bản họp. | AI capabilities provider: Speech-to-Text conversion and Meeting Minutes Summarization/Analysis. |
| 23 | **Salesforce API** | Salesforce Cloud | REST API | N/A | Hệ thống CRM bên ngoài, nơi lưu trữ dữ liệu khách hàng. Office X sẽ đồng bộ thông tin khách và biên bản họp sang đây. | External CRM system storing customer data. Office X syncs customer info and meeting minutes here. |
| 24 | **Stripe API** | External Cloud | REST API | N/A | Cổng thanh toán trực tuyến, xử lý các giao dịch đăng ký gói cước và thanh toán hóa đơn của doanh nghiệp. | Online payment gateway processing subscription transactions and corporate invoice payments. |